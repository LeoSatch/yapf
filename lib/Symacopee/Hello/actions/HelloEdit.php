<?php

/**
 * Description of HelloIndex
 *
 * @author satch
 */
abstract class HelloEdit extends Action {

    public static function run( & $in_aConfig, &$out_aData = array(), &$in_aParameters = array() ) {
        return parent::run( $in_aConfig );
    }

}
